# Changesets

This directory holds changesets that describe upcoming version bumps.

Run `pnpm changeset` to author a new entry, then `pnpm version-packages` to apply pending changes and `pnpm release` to publish.
